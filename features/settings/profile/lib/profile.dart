import 'package:flutter_multi_modular_clean_architecture/main_provider.dart';

/// A Calculator.
class Calculator {
  /// Returns [value] plus 1.
  int addOne(int value) => value + 1;

 var name = getAppName();
 var version = getAppVersion();
}
